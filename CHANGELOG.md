# Changelog

## 0.5.1

- Fall back to `EACHLABS_API_KEY` when an optional `EACH_API_KEY` is blank; trim surrounding key whitespace.
- Refresh vulnerable transitive dependencies within the existing package ranges and rebuild the bundled MCP server.
- Add contributor checks, key-free usage examples, and clearer runtime and credential requirements.
